module.exports = autoprefixer;

function autoprefixer(input, options) {
    var compiled;

	/*options.sourceMap = options.sourceMap !== false;*/
    return require("autoprefixer-core").process(input, options).css;
}

autoprefixer.defaults = {
    accept: ".css",
    ext: ".css"
};
